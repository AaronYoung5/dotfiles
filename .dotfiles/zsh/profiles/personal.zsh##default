#!/usr/bin/env zsh

export PSEUDO_USER="personal"
export TMUX_SESSION_NAME="personal"
export USER_HOME="$PSEUDOS_DIR/personal"

mkdir -p "$USER_HOME"

# Create .envrc if it doesn't exist
if [[ ! -f "$USER_HOME/.envrc" ]]; then
  echo "layout_mamba -n $PSEUDO_USER" > "$USER_HOME/.envrc"
fi
